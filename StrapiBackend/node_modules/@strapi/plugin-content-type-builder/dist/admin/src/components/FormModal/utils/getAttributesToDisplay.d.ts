import type { IconByType } from '../../AttributeIcon';
import type { UID } from '@strapi/types';
export declare const getAttributesToDisplay: (dataTarget: string | undefined, targetUid: UID.Any, nestedComponents: Array<UID.Any>) => IconByType[][];
